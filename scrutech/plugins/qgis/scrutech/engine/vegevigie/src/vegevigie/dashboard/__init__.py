"""Streamlit + leafmap dashboard (M7) — the public VegeVigie demonstrator.

Split UI-free (``data.py``) from rendering (``app.py``) so the discovery/colour
logic is unit-testable without a Streamlit runtime (CLAUDE.md §7, §11).
"""
