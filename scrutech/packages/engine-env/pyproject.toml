[project]
name = "scrutech-engine-env"
version = "0.1.0"
description = "Locate, build and check the ScruTech engine environment (shared by the QGIS plugin and the desktop app)."
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
include = ["engine_env.py"]
