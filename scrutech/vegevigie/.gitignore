# Data — everything the pipeline downloads or produces stays out of git.
data/

# Datacube / DB artifacts wherever they appear
*.zarr/
*.duckdb
*.duckdb.wal

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Notebooks
notebooks/.ipynb_checkpoints/

# QGIS plugin build artifacts (generated by qgis_plugin/package.py — bundled copies
# of the engine + config, and the installable zip). Source lives in src/ and config/.
qgis_plugin/scrutech/vegevigie/
qgis_plugin/scrutech/config/
qgis_plugin/dist/
qgis_plugin/scrutech/ecobuage.py
qgis_plugin/scrutech/sdbpi/
qgis_plugin/scrutech/mini_dc/
